<h1>All Users</h1>
  <% @users.each do |user| %>
	<h3><%= link_to user.username, user_path(user) %></h3>
<ul>
	<li>Admin: <%= check_box_tag 'user', 'admin', user.admin, :disabled => true %></li>
	<li>pages made count</li>
</ul>
<% end %>